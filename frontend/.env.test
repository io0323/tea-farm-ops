# CI環境用テスト設定
REACT_APP_ENVIRONMENT=test
REACT_APP_API_URL=http://localhost:8080
REACT_APP_AUTH_URL=http://localhost:8080/auth

# テスト環境設定
NODE_ENV=test
CI=true

# React Scripts用の設定
BROWSER=none
PORT=3000
HOST=0.0.0.0

# 認証関連の設定（テスト用）
REACT_APP_JWT_SECRET=test-secret-key
REACT_APP_JWT_EXPIRATION=3600000

# API関連の設定
REACT_APP_TIMEOUT=10000
REACT_APP_RETRY_ATTEMPTS=3

# フロントエンドはデータベースに直接接続しないため、DB設定は不要
# バックエンドがpostgresユーザーでDBに接続する
